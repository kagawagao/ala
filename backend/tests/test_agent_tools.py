# REMOVED: entries→file refactor — all in-memory tool tests removed.
# Old tests tested _execute_log_tool, _overview_cache, build_log_index which
# have been deleted. New file-based lazy tools should be tested with temp
# files via integration tests (see test_lazy_log.py for log tool tests).

